import Index from '@/pages/Index'

// MainApp is replaced by the fully-wired app shell in `src/pages/Index.tsx`.
// This component is kept only to preserve the existing `/` route.
export default function MainApp() {
  return <Index />
}

